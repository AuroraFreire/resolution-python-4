[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week4_auroraaa"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week4_auroraaa.main:main"